# json-response-analyzer
A simple JavaScript utility that parses a JSON API response and highlights sensitive fields like tokens and auth flags.  Built to analyze real API responses, not just examples.  Paste your response, see what leaks.
